<footer class="py-8 border-t border-primary/10">
    <div class="max-w-6xl mx-auto px-4">
        <div class="flex flex-col md:flex-row items-center justify-between gap-4">
            <div class="flex items-center gap-2 text-text-secondary text-sm">
                <span>&copy; {{ "now"|date("Y") }} {{ name }}.</span>
                <span>{{ 'footer.rights'|trans }}</span>
            </div>
 
            <div class="flex items-center gap-2 text-text-secondary text-sm">
                <span>{{ 'footer.built'|trans }}</span>
                <span class="text-primary font-medium">Symfony</span>
                <span>&</span>
                <span class="text-accent font-medium">Tailwind CSS</span>
            </div>
        </div>
    </div>
</footer>